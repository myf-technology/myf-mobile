import { StyleSheet } from 'react-native';
import { width } from '../../../constants/responsive';

export default StyleSheet.create({
  input:{
        alignSelf: 'center',
       height: width(140), width: width(60),
        justifyContent: 'center',
      }

})