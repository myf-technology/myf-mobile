import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  cameraOverlay: {
    flex: 1,
    position: 'absolute',
    width: '100%',
    height: '100%',
  },
  svgContainer: {
    flex: 1,
  },
});
